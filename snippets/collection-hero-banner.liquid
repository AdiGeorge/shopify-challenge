{% comment %} Collection hero banner {% endcomment %}
<section class="collectionHero section">
    <h1 class="collectionHero_title">{{collection.title}}</h1>
    {% if collection.image %}
        <img src={{ collection.image | img_url: '1000x' }} />
    {% endif %}
</section>